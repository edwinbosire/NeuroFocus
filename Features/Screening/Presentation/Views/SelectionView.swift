import SwiftUI

// SelectionView scaffold — shows available assessments

struct SelectionView: View {
    @ObservedObject var viewModel: AssessmentViewModel

    var body: some View {
        Text("Selection View — migrate from ContentView.swift")
    }
}
