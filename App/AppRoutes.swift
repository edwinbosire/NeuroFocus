// AppRoutes.swift
// Central route enums for deep-linking and cross-feature navigation.
// TODO: Expand with specific cases as features are added.

import Foundation

enum AppRoute: Equatable {
    case onboarding
    case screening
    case cognitiveTests
    case insights
    case tracking
    case reports
    case education
    case settings
}
